<?xml version="1.0" encoding="utf-8"?>
<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.joelapenna.foursquared"
  android:versionName="2009-04-05"
  android:versionCode="2009040500">

  <uses-permission
    android:name="android.permission.INTERNET"></uses-permission>
  <uses-permission
    android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>

  <application
    android:icon="@drawable/icon"
    android:label="@string/app_name">

    <uses-library
      android:name="com.google.android.maps" />

    <activity
      android:name=".TestActivity"
      android:label="Testing Activity">
      <intent-filter>
        <action
          android:name="android.intent.action.MAIN" />
      </intent-filter>
    </activity>

    <activity
      android:name=".VenueActivity">
      <intent-filter>
        <action
          android:name="android.intent.action.MAIN" />
      </intent-filter>
    </activity>

    <activity
      android:name=".VenueCheckinActivity">
    </activity>

    <activity
      android:name=".VenueInfoActivity">
    </activity>

    <activity
      android:name=".VenueSearchActivity"
      android:label="@string/venue_search_activity_name">
      <intent-filter>
        <action
          android:name="android.intent.action.MAIN" />
      </intent-filter>
    </activity>

    <activity
      android:name=".VenueYelpActivity">
    </activity>

  </application>

</manifest>